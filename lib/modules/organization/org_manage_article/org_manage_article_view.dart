import 'package:flutter/material.dart';

class OrgManageArticleView extends StatelessWidget {
  const OrgManageArticleView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
