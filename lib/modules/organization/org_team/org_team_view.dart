import 'package:flutter/material.dart';

class OrgTeamView extends StatelessWidget {
  const OrgTeamView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
